DATABASE_URL="file:./dev.db"
JWT_SECRET="change-me-in-production"
PORT=4000

# Opcional: quando presente, a Central "Pergunte a IA" usa a API da Anthropic
# para redigir respostas em linguagem natural em cima dos dados reais
# calculados pelo motor de consulta. Sem a chave, o sistema responde com
# um resumo estruturado gerado localmente (sem inventar dados).
ANTHROPIC_API_KEY=
